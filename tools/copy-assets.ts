import * as shell from "shelljs";

// // Copy all the view templates
// shell.mkdir("-p", "dist/mailer/templates");
// shell.cp( "-Rf", "src/mailer/templates", "dist/mailer" );
// // mkdir -p
// shell.mkdir("-p", "dist/public/images");
// shell.cp( "-Rf", "src/public/images", "dist/public" );
